plugins {
    id "com.android.application"
    // START: FlutterFire Configuration
    id 'com.google.gms.google-services'
    // END: FlutterFire Configuration
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.p3"
    compileSdk = 36 // Android SDK version 36
    ndkVersion = "28.2.13676358"
    

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17 // تحديث إلى Java 17
        targetCompatibility = JavaVersion.VERSION_17 // تحديث إلى Java 17
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_17 // تحديث إلى Java 17
    }

    defaultConfig {
        applicationId = "com.example.p3"
        minSdkVersion = flutter.minSdkVersion // Android 6.0 (مناسب)
        targetSdk = 36 // Android SDK version 36
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            // TODO: Add your own signing config for the release build.
            // Signing with the debug keys for now, so `flutter run --release` works.
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}
